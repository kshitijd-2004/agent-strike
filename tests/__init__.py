"""AgentStrike test suite (pytest).

Tests are organised by package layer. Empty stubs are provided so each layer
has an obvious home for new tests as the implementation lands. See
``conftest.py`` for shared fixtures (mock Anthropic client, in-memory event
log, deterministic CSPRNG seed).
"""
