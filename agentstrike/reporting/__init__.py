"""Reporting (Layer 6 — output adapter).

Off-line PDF report generation from a stored event log. Used both at the end
of a CLI run (``agentstrike run --report``) and via the
``GET /api/runs/{session_id}/report`` endpoint.
"""
