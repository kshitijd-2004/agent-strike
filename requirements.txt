# ---------------------------------------------------------------------------
# AgentStrike — Python dependencies
#
# Core:
#   * anthropic     — Official Claude SDK (Red/Blue/Judge agents)
#   * fastapi       — HTTP API + SSE event broadcasting
#   * uvicorn       — ASGI server for FastAPI
#   * sse-starlette — Server-Sent Events helper for FastAPI
#   * mcp           — Model Context Protocol (mock tool environment)
#   * click         — CLI headless runner
#   * pydantic      — Typed event / config schemas
#   * pydantic-settings — Env-driven settings loader
#   * pyyaml        — YAML scenario registry
#   * httpx         — Async HTTP client (used by anthropic + tests)
#   * reportlab     — PDF report generation from event log
#   * python-dotenv — Local development env loading
#
# Dev / test:
#   * pytest         — Test runner
#   * pytest-asyncio — Async test support
#   * pytest-cov     — Coverage reporting
#   * ruff           — Lint + format
#   * mypy           — Static type checking
# ---------------------------------------------------------------------------

anthropic>=0.40.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.0
mcp>=1.0.0
click>=8.1.7
pydantic>=2.9.0
pydantic-settings>=2.5.0
pyyaml>=6.0.2
httpx>=0.27.0
reportlab>=4.2.0
python-dotenv>=1.0.1

pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
ruff>=0.6.0
mypy>=1.11.0
